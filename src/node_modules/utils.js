import {parse} from 'query-string'

export const range = (start, end) => {
  const _start = Number.parseInt(start)
  const _end = Number.parseInt(end)
  return [...Array(_end).keys()].map(k => k + _start)
}

export const limit = 10 // number of articles per page

export const getPaginator = (search) => {
  const parsedSearch = parse(search)
  const currentPage = parsedSearch.page ? Number(parsedSearch.page) : 1
  const offset = currentPage * limit - limit  // 3*10-10=20
  return {currentPage, offset}
}